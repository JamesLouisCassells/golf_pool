// Package golf will hold tournament data polling, name matching,
// and payout calculation logic.
//
// This area is likely to grow the most domain-specific rules, so it is worth
// isolating early instead of mixing golf logic into HTTP handlers.
package golf
